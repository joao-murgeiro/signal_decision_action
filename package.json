{
  "name": "signal-decision-action",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/backend",
    "dev:web": "npm run dev -w apps/frontend",
    "build": "npm run build -ws",
    "typecheck": "npm run typecheck -ws"
  },
  "engines": {
    "node": ">=20"
  }
}

